<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "LatinWiki" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Latinwiki">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+3:wght@400;600;700&family=Source+Serif+4:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet" fetchpriority="high">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script>
      document.addEventListener("turbo:before-visit", function() {
        document.body.classList.remove("wiki-sidebar-open");
      });
    </script>
  </head>

  <body>
    <header class="wiki-mobile-header">
      <button type="button" class="wiki-mobile-menu-btn" aria-label="Open menu" onclick="document.body.classList.toggle('wiki-sidebar-open')">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
      </button>
      <a href="/" class="wiki-mobile-header-title">
        <img src="/icon.png" class="wiki-mobile-header-logo" alt="">
        <span>LatinWiki</span>
      </a>
      <% if current_user %>
        <%= link_to search_path, class: "wiki-mobile-search-btn", aria: { label: "Search" } do %>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        <% end %>
      <% else %>
        <span></span>
      <% end %>
    </header>
    <div class="wiki-sidebar-backdrop" onclick="document.body.classList.remove('wiki-sidebar-open')"></div>
    <% if flash[:alert].present? %>
      <div class="wiki-flash wiki-flash--alert" role="alert">
        <span><%= flash[:alert] %></span>
        <button type="button" class="wiki-flash-close" onclick="this.parentElement.remove()">&times;</button>
      </div>
    <% end %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
